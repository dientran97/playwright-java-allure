# =====================================================================
# TEST environment - activated with -PTEST (the default profile)
# =====================================================================

# --- application under test ------------------------------------------
project.id=SP0308
url=https://bookcart.azurewebsites.net/login

# --- run defaults -----------------------------------------------------
# These are the lowest priority values: a <parameter> of the TestNG xml
# wins over them, and -Dbrowser / -Dheadless on the command line wins
# over everything.
browser=chrome
headless=false
timeout=30
default.user=User1

# --- browser ----------------------------------------------------------
# Leave browser.channel empty to run the Chromium build bundled with
# Playwright; set it to 'chrome' or 'msedge' to drive an installed browser.
browser.channel=
browser.executable.path=
viewport.width=1920
viewport.height=1080
ignore.https.errors=true
slowmo=0

# --- evidence ---------------------------------------------------------
screenshot.on.failure=true
screenshots.clean.before.testcase=true
tracing.enabled=false
