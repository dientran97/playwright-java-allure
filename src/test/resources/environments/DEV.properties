# =====================================================================
# DEV environment - activated with -PDEV
# =====================================================================

# --- application under test ------------------------------------------
project.id=SP0308
# Replace with the real DEV entry point of your application.
url=https://bookcart.azurewebsites.net/login

# --- run defaults -----------------------------------------------------
browser=chrome
headless=false
timeout=30
default.user=User1

# --- browser ----------------------------------------------------------
browser.channel=
browser.executable.path=
viewport.width=1440
viewport.height=900
ignore.https.errors=true
slowmo=0

# --- evidence ---------------------------------------------------------
screenshot.on.failure=true
screenshots.clean.before.testcase=true
tracing.enabled=true
